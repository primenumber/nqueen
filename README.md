NQueen
====

N-Queen solver on CPU and GPU

# build

```
$ make
```

# run

`$ ./solve N M`

`N` is problem size, `M` is expand depth (smaller than `N').
# run

```
$ ./solve 10 5
```

```
$ ./solve 10 5
```
